import MAX_AGE from "./cookieMaxAge";
import removeTokenCookie from "./removeTokenCookie";
import setTokenCookie from "./setTokenCookie";
import getTokenCookie from "./getTokenCookie";

export { MAX_AGE, removeTokenCookie, setTokenCookie, getTokenCookie };
